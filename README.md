# MyGrades API

نظام إدارة درجات الطلاب - واجهة برمجة تطبيقات (API) مبنية باستخدام ASP.NET Core 8.0

## 📝 وصف المشروع

**MyGrades** هو نظام متكامل لإدارة درجات الطلاب في المؤسسات التعليمية. يوفر النظام واجهة برمجة تطبيقات RESTful API لإدارة الطلاب، الدكاترة، المعيدين، المواد الدراسية، الأقسام، والدرجات.

## 🏗️ هيكل المشروع (Clean Architecture)

```
MyGrades/
├── MyGrades.API/                    # طبقة العرض (Presentation Layer)
│   ├── Controllers/                 # API Controllers
│   └── Hubs/                        # SignalR Hubs للإشعارات
├── MyGrades.Application/            # طبقة التطبيق (Application Layer)
├── MyGrades.Application.Contracts/  # العقود والواجهات (DTOs & Interfaces)
├── MyGrades.Domain/                 # طبقة النطاق (Domain Layer)
│   └── Entities/                    # الكيانات
└── MyGrades.Infrastructure/         # طبقة البنية التحتية (Infrastructure Layer)
```

## ✨ المميزات

- 🎓 **إدارة الطلاب**: إضافة، تعديل، حذف، واستيراد الطلاب من ملفات Excel
- 👨‍🏫 **إدارة الدكاترة والمعيدين**: نظام متكامل لإدارة أعضاء هيئة التدريس
- 📚 **إدارة المواد الدراسية**: ربط المواد بالأقسام والمستويات الأكاديمية
- 🏛️ **إدارة الأقسام**: إنشاء وإدارة الأقسام المختلفة
- 📊 **إدارة الدرجات**: تسجي�� درجات الحضور، المهام، والعملي
- 🔐 **المصادقة والتفويض**: JWT Authentication مع ASP.NET Identity
- 📤 **استيراد البيانات**: دعم استيراد البيانات من ملفات Excel (.xlsx, .xls, .csv)
- 🔔 **الإشعارات الفورية**: SignalR للإشعارات في الوقت الحقيقي
- 📄 **توثيق API**: Swagger/OpenAPI

## 🛠️ التقنيات المستخدمة

- **Framework**: ASP.NET Core 8.0
- **Database**: SQL Server with Entity Framework Core 8.0
- **Authentication**: JWT Bearer Token
- **Identity**: ASP.NET Core Identity
- **Real-time**: SignalR
- **Mapping**: AutoMapper
- **Validation**: FluentValidation
- **API Documentation**: Swashbuckle (Swagger)

## 📋 الكيانات الرئيسية

| الكيان | الوصف |
|--------|-------|
| `Student` | بيانات الطالب مع ربطه بالقسم والمستوى الأكاديمي |
| `Doctor` | بيانات الدكتور مع الأقسام والمواد المرتبطة |
| `Assistant` | بيانات المعيد مع القسم والمواد |
| `Subject` | المادة الدراسية مع الدكتور والمعيد المسؤولين |
| `Department` | القسم الأكاديمي |
| `AcademicLevel` | المستوى الدراسي (السنة الدراسية) |
| `Grade` | درجات الطالب (حضور، مهام، عملي) |

## 🚀 البدء السريع

### المتطلبات
- .NET 8.0 SDK
- SQL Server
- Visual Studio 2022 أو VS Code

### التثبيت

1. **استنساخ المشروع**
```bash
git clone https://github.com/MohamedAzoz/MyGrades.git
cd MyGrades
```

2. **تحديث Connection String**
قم بتعديل ملف `appsettings.json` في مجلد `MyGrades.API`:
```json
"ConnectionStrings": {
    "DefaultConnection": "Your_Connection_String_Here"
}
```

3. **تشغيل المشروع**
```bash
cd MyGrades.API
dotnet restore
dotnet run
```

4. **الوصول لـ Swagger**
افتح المتصفح على: `https://localhost:5001/swagger`

## 📡 نقاط النهاية الرئيسية (API Endpoints)

### الطلاب (Students)
- `GET /api/Student` - جلب جميع الطلاب
- `GET /api/Student/{id}` - جلب طالب بالمعرف
- `POST /api/Import/students/import` - استيراد طلاب من Excel

### المواد (Subjects)
- `GET /api/Subject` - جلب جميع المواد
- `GET /api/Subject/{id}` - جلب مادة بالمعرف
- `POST /api/Subject` - إضافة مادة جديدة
- `DELETE /api/Subject/{id}` - حذف مادة

### الأقسام (Departments)
- `GET /api/Department` - جلب جميع الأقسام
- `POST /api/Department/create` - إنشاء قسم جديد

### المستويات الأكاديمية (Academic Levels)
- `GET /api/AcademicLevel/getAll` - جلب جميع المستويات
- `POST /api/AcademicLevel` - إضافة مستوى جديد

### الأدوار (Roles)
- `POST /api/Role` - إضافة دور جديد
- `PUT /api/Role` - تحديث دور
- `DELETE /api/Role/{roleName}` - حذف دور
!
